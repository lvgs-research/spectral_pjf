# Core (required for training + evaluation + analysis)
torch>=2.2
torch_geometric>=2.4
polars>=1.10
numpy>=1.24
scikit-learn>=1.3
scipy>=1.10
pandas>=2.0
matplotlib>=3.7

# Optional
# jieba                  # only for the boundary-validated Tianchi graph build
#                        # (python -m link_prediction_experiment.tianchi_pjf.build
#                        #  --boundary-validate); the default substring miner needs nothing
# sentence-transformers  # only to embed REAL text with the frozen encoders, i.e. the
#                        # embed_text pipeline (tianchi_pjf.embed_text / embed_text_tech).
#                        # Not needed for --dry-run, training, or the smoke test.
